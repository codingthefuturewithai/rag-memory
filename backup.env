# Backup configuration for docker-volume-backup sidecar (local development only)
# Not used on Fly.io - relies on native volume snapshots instead

# Cron schedule for backups (default: 2 AM daily, adjust as needed)
# Format: "0 2 * * *" means every day at 2:00 AM local time
# Examples:
#   "0 3 * * *" = 3 AM daily
#   "0 0 * * *" = midnight daily
#   "0 2 * * 0" = 2 AM Sunday only
#   "*/30 * * * *" = every 30 minutes
BACKUP_CRON_EXPRESSION=0 2 * * *

# Number of days to retain backups before auto-delete
BACKUP_RETENTION_DAYS=7

# Backup filename format (supports strftime placeholders)
# Available: %Y (year), %m (month), %d (day), %H (hour), %M (minute), %S (second)
BACKUP_FILENAME=rag-memory-backup-%Y%m%d-%H%M%S.tar.gz

# Alternative: S3 backup storage (optional - comment out to use local filesystem)
# Uncomment and configure if you want to backup to S3 in addition to local
# BACKUP_BACKEND=s3
# BACKUP_S3_BUCKET=your-bucket-name
# BACKUP_S3_PREFIX=rag-memory-backups/
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_DEFAULT_REGION=us-east-1
