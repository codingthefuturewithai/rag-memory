# ================================================================================
# TEST ENVIRONMENT - .env.test
# ================================================================================
# Used ONLY by pytest via conftest.py.
# Points to ephemeral test servers (docker-compose.test.yml).
# Data is INTENTIONALLY NOT PERSISTENT - pytest cleanup deletes all test data.
#
# DO NOT use this file for development/manual testing.
# DO NOT commit real credentials to this file.
#
# USAGE:
#   pytest automatically loads this via tests/conftest.py
#   DO NOT manually source this file
# ================================================================================

# OpenAI API Key (required for embedding generation in tests)
OPENAI_API_KEY=sk-your-api-key-here

# ================================================================================
# PostgreSQL - Test Instance (docker-compose.test.yml)
# ================================================================================
POSTGRES_HOST=localhost
POSTGRES_PORT=54321
POSTGRES_USER=raguser
POSTGRES_PASSWORD=ragpassword
POSTGRES_DB=rag_memory_test

DATABASE_URL=postgresql://raguser:ragpassword@localhost:54321/rag_memory_test

# ================================================================================
# Neo4j - Test Instance (docker-compose.test.yml)
# ================================================================================
NEO4J_URI=bolt://localhost:7688
NEO4J_USER=neo4j
NEO4J_PASSWORD=test-password

# ================================================================================
# PRODUCTION PROTECTION - Test Mode
# ================================================================================
# These are EPHEMERAL test servers - data cleaned up after each test run
ENV_NAME=test
ENVIRONMENT=test
