# ================================================================================
# SUPABASE/PRODUCTION ENVIRONMENT - .env.supabase
# ================================================================================
# Use ONLY when you explicitly want to test against Supabase production database.
# This is a DEVELOPMENT-level testing setup using your PRODUCTION database.
#
# ⚠️  DANGER: This connects to your ACTUAL production database in Supabase!
# ⚠️  Data created with this config is REAL and PERSISTS permanently!
# ⚠️  Never use this for automated tests!
# ⚠️  Only use this for manual development-level testing when explicitly needed.
#
# USAGE:
#   export $(cat .env.supabase | grep -v '^#' | xargs)
#   uv run rag search "query"
#
# Or automatically:
#   source .env.supabase && uv run rag search "query"
#
# SWITCHING BACK TO LOCAL:
#   source .env.dev
# ================================================================================

# OpenAI API Key (required for all operations)
OPENAI_API_KEY=sk-your-api-key-here

# ================================================================================
# PostgreSQL - Supabase Production
# ================================================================================
# Use Session Mode (port 5432) for persistent connections
# See .env.example for other Supabase connection modes
#
# To find your credentials:
#   1. Go to Supabase Dashboard
#   2. Project Settings → Database
#   3. Get connection string and extract components
#
# Connection Modes Available:
#   - Session Mode (port 5432): Best for RAG Memory (RECOMMENDED)
#   - Transaction Mode (port 6543): For serverless functions
#   - Direct Connection: For pg_dump/restore only
#
DATABASE_URL=postgresql://postgres.yjokksbyqpzjoumjdyuu:[YOUR-PASSWORD]@aws-0-us-east-1.pooler.supabase.com:5432/postgres

# ================================================================================
# Neo4j - Local Development Instance
# ================================================================================
# When using Supabase for Postgres, still use local Neo4j for development.
# You can adjust this if you have a separate Neo4j production instance.
#
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=dev-password

# ================================================================================
# PRODUCTION PROTECTION - Supabase Mode
# ================================================================================
# Flag to indicate this is production database with dev-level testing
ENV_NAME=supabase-dev
ENVIRONMENT=production-data-dev-testing

# ⚠️  WARNING LOG MESSAGE (displayed by config_loader.py on startup)
# This environment flag helps prevent accidental data destruction
